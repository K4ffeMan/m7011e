import os
import random
import time
import requests
import string
url = os.getenv('ORDER_SERVICE_URL', 'http://localhost:5000')
headers = {"X-Test-Mode": "true"}
youtube_link = string.ascii_letters + string.digits
videos = []
def random_youtube_id():
    link = "".join(random.choice(youtube_link) for _ in range(11))
    return f"https://www.youtube.com/watch?v={link}"
def generate_http_traffic():
    roomId = None
    while True:
        try:
            if roomId is None:
                #Create room
                response = requests.post(f"{url}/api/rooms/", headers=headers)
                if response.status_code == 201 or response.status_code == 200:
                    roomId = response.json().get("roomId")
                    print("room created")
            else:
                
                #get room
                requests.get(f"{url}/api/rooms/{roomId}")
                print("successfully got room")
                #Add videos
                for _ in range(3):
                    video_url = random_youtube_id()
                    body = {"url": video_url}
                    response = requests.post(f"{url}/api/videos/{roomId}", json=body, headers=headers)
                    print("video added")
                time.sleep(0.3)
                res = requests.get(f"{url}/api/videos/{roomId}")
                videos = res.json()
                print("got the videos")
                video_ids = []
                for video in videos:
                    video_ids.append(video["id"])
                #Start vote
                requests.post(f"{url}/api/vote/start/{roomId}", headers=headers)
                print("vote started")
                #Vote
                for _ in range(5):
                    requests.post(f"{url}/api/vote/{roomId}/{random.choice(video_ids)}", headers=headers)
                    print("voted")
                #End vote
                requests.post(f"{url}/api/vote/end/{roomId}", headers=headers)
                print("vote ended")
                #requests.delete(f"{url}/api/rooms/{roomId}")
                roomId = None
                time.sleep(0.3)
        except Exception:
            return
if __name__ == "__main__":
    generate_http_traffic()